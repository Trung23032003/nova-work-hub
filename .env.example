# ===========================================
# DATABASE (Supabase PostgreSQL)
# ===========================================
# Transaction pooler - dùng cho app runtime (port 6543)
DATABASE_URL="postgres://postgres.xxx:[YOUR_PASSWORD]@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection - dùng cho Prisma CLI migrate, push (port 5432)
DIRECT_URL="postgres://postgres.xxx:[YOUR_PASSWORD]@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres"

# ===========================================
# AUTHENTICATION (Auth.js v5)
# ===========================================
# Secret key để mã hóa JWT và session cookies
# Generate bằng: openssl rand -base64 32
# HOẶC dùng giá trị mẫu dưới đây cho development:
AUTH_SECRET="novawork-hub-super-secret-key-for-development-only-32chars"

# URL của application
AUTH_URL="http://localhost:3000"

# ===========================================
# GOOGLE OAUTH (Optional)
# ===========================================
# Bỏ qua nếu chỉ dùng Credentials login
# Lấy từ: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
